[project]
name = "chat-agent"
version = "0.1.0"
description = "CLI chat agent with memory"
requires-python = ">=3.8"
dependencies = [
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v"
